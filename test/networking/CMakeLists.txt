SET(TARGET_SRC
    tcp_messaging.cpp 
)
SET(TARGET_H
    # header.h
)

SETUP_APPLICATION(tcp_messaging Tests)

# ---------------

SET(TARGET_SRC
    parallel_connections.cpp 
)
SET(TARGET_H
    # header.h
)

SETUP_APPLICATION(parallel_connections Tests)
